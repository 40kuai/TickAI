# 工作记录

> 本文件记录每次完成的工作、下一步工作与未完成事项(用户规则 #1)。

## 2026-08-21 — 飞书机器人接入

### 已完成

- **Task 1** (commit `0e5daca`):`requirements.txt` 添加 `lark-oapi>=1.5.0,<2.0.0`;`hermes/config/settings.py` 新增 `FEISHU_APP_ID()` / `FEISHU_APP_SECRET()` / `FEISHU_OPENID_WHITELIST()` / `FEISHU_ENABLED()`;`.env.example` 新增飞书配置模板。
- **Task 2** (commit `5a770c8`):飞书事件解析与白名单校验(纯函数)。
  - 新增 `hermes/feishu/__init__.py`(空)、`hermes/feishu/handler.py`(纯函数:`extract_message_text` / `get_open_id` / `is_whitelisted` / `should_respond`,不依赖 lark SDK)
  - 新增 `tests/feishu/__init__.py`(空)、`tests/feishu/test_handler.py`(11 个用例)
  - 严格 TDD:先写失败测试 → 确认 RED(ModuleNotFoundError)→ 实现 → 确认 GREEN(11 passed)
- **Task 3** (commit `adb59f9`):飞书消息分发器(队列 + 会话映射)。
  - 新增 `hermes/feishu/bot.py`(`FeishuBot`:白名单校验 + 立即回复「处理中」+ 入队 + open_id→conversation_id 线程安全映射,不依赖 lark SDK,发送/回复函数构造注入)
  - 新增 `tests/feishu/test_bot.py`(5 个用例)
  - 严格 TDD:先写失败测试 → 确认 RED(ModuleNotFoundError)→ 实现 → 确认 GREEN(5 passed);`tests/feishu/` 全量 16 passed,无回归
- **Task 5 修复** (本 commit):飞书 ws SDK API 兼容修复 + 重试测试。
  - `hermes/feishu/ws.py`:① `reply_message` 的 builder 调用 `.body(body)` → 改为 `.request_body(body)`(lark-oapi 1.7.3 无 `body` 方法,群聊回复必崩);② `stop_feishu_bot` 不再调用不存在的 `ws.Client.stop()`,仅释放引用并记日志(daemon 线程随进程退出);③ 删除无用死代码 `_cleanup_for_tests()`
  - `tests/feishu/test_ws.py`:新增 `_send_with_retry` 的 2 个用例(失败重试 2 次共 3 次尝试后放弃 / 成功只调用 1 次)
  - 验证:SDK 实测 `ReplyMessageRequest.builder()` 方法仅 `['build', 'message_id', 'request_body']`、`lark.ws.Client` 仅 `['start']`;`tests/feishu/` 全量 26 passed,无回归
- **Task 4** (commit `5c95e50`):飞书后台 worker。
  - 新增 `hermes/feishu/tasks.py`(`FeishuWorker.process_task` 调 `chat()` 并按场景回复,`run_forever` 阻塞消费队列;`chat_fn`/发送/回复/会话回写全注入,不依赖 lark SDK)
  - 新增 `tests/feishu/test_tasks.py`(3 个用例)
  - 计划偏差:计划实现为 `"处理出错: {exc}"` 但测试断言含 `"错误"`,已按测试改为 `"处理错误: {exc}"`(最小修正)
- **Task 6** (本阶段):依赖确认 + 全量测试。
  - `lark-oapi 1.7.3` 已安装(满足 `>=1.5.0,<2.0.0`),`tests/feishu/` 28 passed,包导入验证 `feishu ok`
  - 全量 `289 collected, 224 passed, 65 failed, 2 errors` —— **全部失败为项目既有问题**,与飞书工作无关(根源:Server 模型缺 username 字段、llm.py 缺 `_log_llm_error`、services.py 缺 `system_tools`、i18n import 缺失)
- **最终审查修复**:工作区 `hermes/feishu/ws.py` 曾回退 c8a4a4a 的三处修复导致 2 测试失败,已按用户确认恢复 HEAD 版本,`tests/feishu/` 28 passed。

### 环境变更(注意)

- 项目 `.venv` 原本未安装 pytest,为运行测试已用清华镜像安装 `pytest==8.4.2`(与系统 python3 版本一致)。未修改 `requirements.txt`(pytest 仅测试依赖,可按需由父 agent 决定是否加入)。

### 已知问题(非本任务引入,需后续处理)

- 运行完整测试套件时,`tests/core/test_llm.py` 与 `tests/tools/ssh/test_services.py` 存在 collection 错误:`hermes/core/llm.py` 无 `_log_llm_error`、`hermes/tools/ssh/services.py` 无 `system_tools`。均为仓库既有失败,与飞书模块无关,未改动。
- 工作区存在未提交的删除:`docs/superpowers/plans/2026-06-17-hermes-refactor.md`(D 状态,非本任务范围,未处理)。

### 下一步

- [x] **Task 4**:实现 `hermes/feishu/tasks.py` 后台 worker + `tests/feishu/test_tasks.py`(已完成,commit `5c95e50`)
- [x] **Task 5**:实现 `hermes/feishu/ws.py` 长连接客户端 + `api/main.py` 启动接入 + `tests/feishu/test_ws.py`(已完成,commit `702bd67` / `2587fe4` / `c8a4a4a`)
- [x] **Task 6**:安装 `lark-oapi` 依赖并运行全部测试(已完成,28/28 通过)
- [ ] **Task 7**:端到端冒烟验证(手动,需真实飞书凭据)—— 待用户提供 OpenID 后执行

## 2026-08-21 — 飞书 ws.py 代码评审修复(惰性导入 + 幂等保护)

### 已完成(本 commit)

- **Issue 1(Important)**:`lark-oapi` 不再于模块加载期硬依赖。
  - `hermes/feishu/ws.py`:模块级 `import lark_oapi as lark` 用 `try/except ImportError` 包裹,未安装时 `lark = None`,模块本身永不因缺 SDK 崩溃(纵深防御)。
  - `api/main.py`:删除顶层 `from hermes.feishu.ws import ...`,改为在 `startup()`/`shutdown()` 事件处理器内部惰性导入 `start_feishu_bot` / `stop_feishu_bot`。即使 `FEISHU_ENABLED=False`,`api.main` 模块加载也不会触碰 lark-oapi。
- **Issue 2(Important)**:`start_feishu_bot()` 增加幂等保护。
  - `FEISHU_ENABLED` 检查通过后,若 `_bot is not None` 直接记日志并返回,避免 uvicorn --reload 或重复调用产生第二个 ws 连接(超出 lark 单应用连接数限制)。
  - `stop_feishu_bot()` 在释放 `_ws_client` 的同时重置 `_bot = None`、`_worker_thread = None`,保证可再次启动。
- **测试更新**(`tests/feishu/test_ws.py`):
  - `StartFeishuBotTests` 增加 `setUp`/`tearDown` 重置模块级单例,避免用例间相互干扰。
  - 新增 `test_second_call_is_idempotent_skips_restart`:二次调用不重复构建 bot/不重复拉线程。
  - 新增 `test_stop_resets_module_globals`:stop 后三个全局变量均置 None。
- **验证**:`tests/feishu/` 全量 28 passed;`ast.parse` syntax ok;`hermes.feishu.ws` 导入正常(lark 为 module);`api.main` 模块级导入正常。

### 备注

- `api/main.py` 的 `on_event` 弃用提示为既有 Hint,非本任务范围,未改动。
- 工作区仍存在未提交的删除 `docs/superpowers/plans/2026-06-17-hermes-refactor.md`(D 状态,非本任务范围)。

### 下一步

- Task 7 端到端冒烟验证(手动,需真实飞书凭据)。

## 2026-08-21 — 冒烟测试发现并修复:lark 与 uvicorn 事件循环冲突

### 问题

`uvicorn api.main:app` 启动后报 `RuntimeError: this event loop is already running`,飞书长连接无法建立。根因实验确认:

- `lark_oapi.ws.client` 在**模块导入时**执行 `asyncio.get_event_loop()` 捕获事件循环并全局固定。
- uvicorn 用 **uvloop** 主循环;`api/main.py` startup 在主线程 import `lark_oapi`(连带加载 ws.client)时,捕获到**正在运行的 uvloop 主循环**。
- 之后 `ws.Client.start()` 内部对运行中的循环调 `run_until_complete()` → 抛错。
- 实验:主循环运行时 import → 模块级 loop 是 `<uvloop.Loop running=True>`;干净线程 import → `<_UnixSelectorEventLoop running=False>`。

### 修复(commit `110a0e8`)

- `hermes/feishu/ws.py`:
  - 移除模块级 `import lark_oapi`,改为 `_import_lark()` **延迟导入**。
  - 新增模块级 `_start_ws(app_id, app_secret, bot)`:在**专用干净线程**中首次 import lark 并启动 `ws.Client.start()`(阻塞)。
  - `send_message`/`reply_message` 内通过 `_import_lark()` 惰性获取 lark 与 api client。
- `tests/feishu/test_ws.py`:patch 目标从 `ws.lark` 改为 `_import_lark`;新增 `StartWsTests` 覆盖 `_start_ws` 的正常启动与失败兜底;线程断言改为 `_start_ws`。
- 结果:`tests/feishu/` 30 passed。

### 验证(真实运行)

`uvicorn api.main:app --port 8010` 启动后日志:

```
[Lark] [INFO] connected to wss://msg-frontier.feishu.cn/ws/v2?... [conn_id=...]
```

**长连接真实连上飞书,错误消失。** 机器人已在线,待用户发消息抓 open_id 填入白名单。

### 备注

- 端口 8000 有 2 个旧 Python 进程占用(用户此前启动的后端),本次冒烟验证用 8010 端口,未杀旧进程。
- 调试日志:`hermes/feishu/bot.py` 在 `handle_event` 开头打印 `收到飞书消息: open_id=...`(commit `42dff37`),用于首次配置白名单时获取 open_id。

## 2026-08-21 — 飞书机器人上线 + 模型地址修复 + skill 能力接入

### 已完成

1. **飞书机器人正式上线**(冒烟验证通过):
   - 事件已真实收到:lark SDK `receive message, event_type: im.message.receive_v1`,`sender.open_id=ou_b86775db55b073aa9746fb6229130a32`(已填入 `.env` 白名单)。
   - 根因排查:此前"发消息没反应"是 **白名单为空 + Python logging 默认 WARNING 吞掉 INFO 日志** 双重原因,并非事件没收到。
   - 修复:`api/main.py` 在 startup 时 `logging.getLogger("hermes.feishu").setLevel(logging.INFO)`;`.env` 填入真实 open_id。
2. **TOKENHUB 模型地址修复**:
   - 问题:LLM 调用报 `Expecting value: line 1 column 1 (char 0)`(非 JSON)。
   - 根因:`.env` 的 `TOKENHUB_BASE_URL=https://aiproxy.nanshe-arch.com/` 缺 `/v1` 前缀且带尾斜杠,代码拼出 `//chat/completions` 或非 `/v1` 路径,网关返回 HTML 首页。
   - 修复:改为 `TOKENHUB_BASE_URL=https://aiproxy.nanshe-arch.com/v1`,curl 实测返回合法 JSON。
3. **飞书对话具备 skill 能力**(新增 `run_skill` 对话工具):
   - 新增 `hermes/tools/skills/__init__.py` + `hermes/tools/skills/tools.py`:注册 `run_skill` 工具(schema 枚举 skill 名,handler 调 `skill_runner.run_skill()` 并回读 `SkillOutcome.findings_summary` 返回分析结论)。
   - 自动发现机制生效:`import hermes.tools` 后 registry 共 15 个工具,含 `run_skill`。
   - 新增 `tests/tools/skills/test_run_skill.py`(6 用例)。
4. **修复 server_id 为 NULL 的 SAWarning**:
   - `hermes/agents/chat.py`(`chat()` 与 `chat_stream()` 两处):`persist_tool_run` 仅在 `server_id` 为有效正整数时落库,否则跳过(此前 `args.get("server_id") if isinstance(...int) else None` 会把非 int/缺失传入 `session.get(Server, None)` 触发警告)。
5. **系统提示词强化**(`chat()` 与 `chat_stream()` 两处):新增规则 5(server_id 必须是数据库整数 ID,不确定先查 `list_servers`,禁止编造)与规则 6(用户要求运行 skill 时调用 `run_skill` 工具)。

### 验证

- `tests/tools/skills/` 6 passed;`tests/feishu/` 28 passed;`tests/agents/test_chat.py` 中 2 个失败为既有问题(`Server` 模型缺 `username` 字段),与本次改动无关。
- `uvicorn api.main:app --port 8020`(进程 23304)已重启,长连接已连上,新代码生效。

### 下一步

- 在飞书里实测:"跑一下 detect_oom_killed skill" 验证 skill 链路;顺带验证 server_id 警告消失。
- 待办:`docs/superpowers/plans/2026-06-17-hermes-refactor.md` 被误删(1028 行,D 状态,非本次任务),需用户确认是否恢复。
- 待办:项目 65 个既有测试失败(Server 模型缺 username 等),建议后续单独清理。

## 2026-08-21 — 管理员登录失败(症状 B)修复

### 问题

用户报"登录失败，请检查用户名和密码"(症状 B)。诊断结论:

1. **直接原因**:后端 FastAPI 服务**未运行**(8000 端口无任何监听),前端登录请求打不到后端 → 前端统一报"登录失败,请检查用户名和密码"。数据库数据本身正确。
2. **潜在 bug(一并修复)**:`api/deps.py` 的 `JWT_SECRET` 只从 `os.environ` 读取,不读 `.env` → 未设置环境变量时每次重启随机生成 → 所有已登录会话 token 全部失效(用户表现为"管理员用户不对了")。

### 证据

- 数据库 `data/opsticket.db`:admin 用户存在、`is_active=1`,bcrypt hash 验证 **admin123 匹配**(密码本身正确)。
- 重启前 `lsof -i :8000` 空,`ps` 无 uvicorn 进程;`/tmp/fastapi.log` 曾有 `Address already in use`(旧进程未清干净导致上次启动失败,现已自然退出)。
- 修复后实测:POST `/api/auth/login` 返回 `{"id":1,"username":"admin","is_active":true,...}`,`Set-Cookie` 下发 `access_token` JWT,登录链路完全打通。

### 修复内容

- `api/deps.py`:第 28 行 `JWT_SECRET = os.environ.get("JWT_SECRET") or secrets.token_urlsafe(32)` → 改为经 `hermes.config.settings.get("JWT_SECRET")` 读取(env 优先、其次 .env、最后随机兜底)。验证:`from api.deps import JWT_SECRET` 输出 `tickai-jwt-secret-20...`(即 .env 固定值),不再随机。

### 服务状态

- `./restart.sh` 重启成功:后端 `http://localhost:8000`、前端 `http://localhost:5173` 均监听正常。

### 下一步

- 验证前端页面实际登录(admin/admin123)成功。
- 待办:Prometheus 真实配置值(ARMS URL/token)仍为空,需用户提供。
- 待办:`docs/superpowers/plans/2026-06-17-hermes-refactor.md` 误删待恢复。
- 待办:65 个既有测试失败待清理。

## 2026-08-21 — 飞书日志落盘到 logs/ + 登录修复说明

### 问题

用户问"飞书相关日志是否打到 logs 下,方便排查"。现状:项目已有 `hermes/core/logging.py`(TimedRotatingFileHandler 按天轮转、默认 `logs/app.log`、保留 7 天),但**从未被调用**,飞书/lark 日志只打到 `/tmp/fastapi.log`(uvicorn stdout 重定向),进程重启即丢失。

### 修复

- `api/main.py` 的 `startup()` 最前面调用 `hermes.core.logging.setup_logging(level=logging.INFO)`(惰性导入)。此后后端 + 飞书(lark、hermes.feishu)日志全部写入 `logs/app.log`,按天轮转保留 7 天,同时保留控制台输出。
- 验证:重启后 `logs/app.log` 出现 `飞书长连接已启动`、`connected to wss://msg-frontier.feishu.cn/ws/v2...`、事件消息等完整飞书日志,带时间戳/logger 名/级别。
- 备注:日志中出现两次"启动/停止"是 uvicorn `--reload` 的 reload 进程在 restart 瞬间的重复触发(已有幂等保护,最终单连接稳定)。若要彻底消除,可去掉 `--reload` 或改独立飞书进程(待用户定夺)。

## 2026-08-21 — 优化 diagnose_prometheus_anomaly skill 的触发可靠性

### 问题

用户要求"优化 diagnose_prometheus_anomaly skill，保证查询 nfc 相关服务的状态或监控情况调用此 skill"。排查对话链路发现机制性瓶颈:

- 对话 LLM(Web/飞书)通过 `run_skill` 工具触发 skill,但 `hermes/tools/skills/tools.py` 的 schema 只给 LLM **skill 名字枚举**,不带各 skill 的用途描述 → LLM 看到 `diagnose_prometheus_anomaly` 名字无法确定它就是"nfc 服务巡检"入口,选型不可靠。

### 改动(三层,内容 + 机制)

1. **skill 文件** `hermes/skills/library/diagnose_prometheus_anomaly.md`:
   - frontmatter `description` 改为多行,明确"nfc 系列服务健康状态/监控巡检的唯一入口",列出触发维度(服务请求/数据库/SQL/JVM/系统)。
   - `Goal` 强调"本 skill 是 nfc 服务状态/监控巡检的唯一入口"。
   - `When to use` 扩充触发场景(状态/健康/监控/性能/异常/告警/挂了/失败率/CPU/JVM/GC/慢 SQL/网络等措辞),并提示不要因措辞变化漏选、关键词含 nfc/ARMS/Prometheus/巡检/健康检查等即触发。
2. **工具 schema** `hermes/tools/skills/tools.py` `_skill_schema()`:
   - 注入「skill 名 → frontmatter description」映射到工具 description 与 `skill_name` 参数描述。
   - 显式规则:"用户查询 nfc 服务状态/健康/监控/异常/巡检时,必须选择 diagnose_prometheus_anomaly"。
3. **系统提示词** `hermes/agents/chat.py`(chat 与 chat_stream 两处):新增规则 7——nfc 服务状态/健康/监控/性能/异常/告警/数据库/JVM/GC/CPU/慢 SQL/网络问题必须调用 `run_skill(skill_name="diagnose_prometheus_anomaly")`。

### 验证

- `_skill_schema()` 实测:工具与参数描述均包含两个 skill 的用途映射与"nfc → diagnose_prometheus_anomaly"的强制规则;enum 正确。
- `tests/tools/skills/` 全部通过;`tests/agents/test_chat.py` 仅 2 个既有失败(Server 模型缺 username,与本次无关)。
- `./restart.sh` 已重启,改动生效。

## 2026-08-21 — 修复 skill 执行环境缺 Prometheus 工具(monitoring toolset)

### 问题

用户实测 `diagnose_prometheus_anomaly` skill,结果报"无法访问生产集群,未发现 nfc 服务"。用户指出:nfc 服务**不在 k8s 中**,是通过 **Prometheus(ARMS)监控指标**分析的。

### 根因

skill 内部 LLM 的工具集由 `hermes/agents/skill_runner.py` 的 `SKILL_TOOLSETS` 决定,原值为 **`{"k8s"}`** —— 只含 k8s 只读工具。而 Prometheus 查询工具的 toolset 是 **`"monitoring"`**(`hermes/tools/prometheus/tools.py` 注册时 `toolset="monitoring"`)。因此 skill 执行时 LLM **手里没有** `prometheus_instant_query`/`prometheus_range_query`,只能去查 k8s → 错误得出"没找到 nfc 服务"。

### tools 与 skill 优先级机制(梳理)

- 注册:`hermes/tools/registry.py` 每个工具带 `toolset`(k8s/monitoring/ops/system/default 等)。
- 对话(Web/飞书):`hermes/agents/chat.py` 用 `registry.list_schemas()` 全量工具给 LLM。
- Skill 执行:`skill_runner._skill_tools()` 只给 `SKILL_TOOLSETS` 内的工具(k8s+monitoring)。
- `run_skill` 是代理工具:对话 LLM 决定"跑哪个 skill"→ 委托 SkillRunner → 内部 LLM 用受限工具集执行分析(两次独立 LLM 调用)。
- 工具注册由 `hermes/tools/__init__.py` 的 `auto_register_tools()` 自动发现加载(`import hermes.tools` 触发,chat.py 已引入)。

### 修复

`hermes/agents/skill_runner.py`:`SKILL_TOOLSETS = {"k8s"}` → `{"k8s", "monitoring"}`,并注释说明原因。

### 验证

- 模拟 api 启动路径(`import hermes.tools` 自动发现)后,`_skill_tools()` 含 8 个工具,k8s 6 个 + prometheus 2 个,`prometheus_instant_query`/`prometheus_range_query` 均在内。
- `.env` 已有 `PROMETHEUS_URL`(ARMS cn-beijing)与 `PROMETHEUS_TOKEN`,可真实查询。
- `tests/agents/test_skill_runner.py` 8 个失败为**既有**接口不匹配(测试调 `runner.run("test_skill")` 传字符串,而 `run()` 期望 AgentContext),与本次改动无关。
- `./restart.sh` 已重启生效。

## 2026-08-21 — 缓解 skill 超时/(max tool rounds reached),默认查询最近 10 分钟

### 问题

用户实测 `diagnose_prometheus_anomaly` 仍易超时或触顶 `(max tool rounds reached)`。原因:

1. `prometheus_range_query` 默认 `start` 是 **1 小时前**(`now - 3600`),范围大、数据点多,复杂聚合(PromQL 带 `sum_over_time_lorc`/`irate`)响应慢。
2. skill 文档未约束时间范围与查询数量,LLM 倾向对 5 个维度逐一做范围查询,轮次消耗大。

### 修复(两处配合)

1. **`hermes/tools/prometheus/tools.py`**:
   - `prometheus_range_query` 默认 `start` 从 `now - 3600`(1 小时)→ `now - 600`(**10 分钟**);schema 描述同步改为"默认 10 分钟前"。
2. **`hermes/skills/library/diagnose_prometheus_anomaly.md`**:
   - 新增「⏱️ 默认时间范围与查询性能(重要,先读)」章节:默认只查最近 10 分钟;优先即时查询;每维度最多 1 条聚合查询;发现 critical 直接报告;连续空/超时停止并如实说明。
   - Edge cases 补充:同一查询连续超时/失败 2 次以上跳过、单查询失败不中止整个 skill 但不过度重试。
   - What NOT to do 补充:不要为"跑完所有检查"反复查询。

### 验证

- `load_skill('diagnose_prometheus_anomaly')` 解析正常,body 含"最近 10 分钟"策略。
- `_RANGE_QUERY_SCHEMA` start 描述 = "默认 10 分钟前";handler 默认 `now - 600`。
- `./restart.sh` 已重启生效。

## 2026-08-21 — 根治 (max tool rounds reached)

### 问题

用户实测 `diagnose_prometheus_anomaly` 仍报 `(max tool rounds reached)`。根因:skill 内部 LLM 循环被 `SkillRunner.max_tool_rounds` 限制为 **5 轮**(`hermes/agents/skill_runner.py`)。skill 有 5 个检查维度,每维度 1-2 次 PromQL 查询,5 轮极易耗尽;且 LLM 倾向"查完再收尾",常常第 6 轮才给结论,轮次不够。

### 修复(三层)

1. **提高轮次上限**:`max_tool_rounds` 默认 5 → **15**(`SkillRunner.__init__`)。5 维度 × 每维度 1-2 查询 + 收尾,15 轮足够。
2. **提示词强制收敛**:`execute_skill` 的 system_prompt 新增效率要求——多查询在**同一轮并行发起**;有足够数据立即给结论;不要为"报告完整"重复查数据(配合 skill 文档的"每维度最多 1 条查询、发现 critical 即报告")。
3. **轮次耗尽兜底**:`_run_llm_loop` 在轮次耗尽时,不再返回空串,而是**不带工具**再调一次 LLM,基于已有 tool 结果(messages 最近 8 条)强制生成总结;失败才退回 `(max tool rounds reached...)`。用户至少能拿到部分结论。

### 验证

- `py_compile` 通过;默认 `max_tool_rounds = 15` 确认。
- Mock LLM 验证兜底:LLM 持续调工具不收敛 → 轮次耗尽后触发兜底总结,findings_summary 输出"基于已有结果:服务正常"(而非报错串)。
- `./restart.sh` 已重启生效。

## 2026-08-21 — 新增 prometheus_service_health 聚合工具(根治 nfc 巡检轮次耗尽)

### 问题

用户实测:查询 nfc 服务 prometheus 状态时仍易报 `(max tool rounds reached)`。根因:即使提高了轮次上限,LLM 仍要**逐维度多次调用** `prometheus_instant_query`(5 个维度 × 多次),每次工具调用消耗一轮,容易触顶。

### 方案(用户建议)

将 prometheus 相关查询**整合为一个通用的聚合工具**,一次调用并行获取指定服务的所有核心指标,避免 LLM 多次调用。

### 实现

`hermes/tools/prometheus/tools.py` 新增第 3 个工具 **`prometheus_service_health`**:

- 参数:`service`(必填,过滤正则如 `nfc-.*`)、`dimensions`(可选,枚举 request/database/jvm/system,默认全部)、`start`/`end`(默认最近 10 分钟)。
- 一次调用内按维度执行 `_DIMENSION_QUERIES` 定义的核心 PromQL(共 11 条:request 3 + database 3 + jvm 1 + system 4),返回结构化快照 `{service, time_range, dimensions: {dim: {metric: {query, result_count, results, truncated}}}, hint}`。
- 结果截断到 20 条避免上下文过大;单条失败/空不报错,由 hint 提示"空表示无数据,不要误判为正常"。
- 注册 toolset=`monitoring`,自动进入 skill 工具集。

skill 文档 `diagnose_prometheus_anomaly.md`:

- How to use 更新为三个工具,`prometheus_service_health` 标为**首选**。
- 新增「✅ 推荐流程(避免轮次耗尽)」:先调用聚合工具拿快照,仅当某维度异常时再用 instant/range 深入定位,通常 1-2 次工具调用即可出报告。
- Checking steps 开头强调先调用一次 `prometheus_service_health(service="nfc-.*")` 再按维度分析。

### 验证

- TDD:`tests/tools/prometheus/test_service_health.py` 7 个用例全过(schema 必填/维度枚举/默认时间/缺参报错/聚合结果/调用次数=11/空结果不报错)。
- skill 工具集实测含 `prometheus_service_health`(共 9 个工具);skill 文档解析正常,含聚合工具推荐。
- `tests/tools/prometheus/` + `tests/tools/skills/` 共 15 passed,无回归。
- `./restart.sh` 已重启生效。

## 2026-08-21 — 删除两个零散 Prometheus 工具,整合为单一工具(用户要求)

### 需求

用户明确要求:不再要 `prometheus_instant_query` 和 `prometheus_range_query`。**所有**服务 prometheus 相关监控/状态查询统一整合为单一工具,避免多轮工具查询。

### 实现

`hermes/tools/prometheus/tools.py`:

- **删除** `prometheus_instant_query`、`prometheus_range_query` 两个工具(含 schema、handler、注册)。
- **保留唯一工具 `prometheus_service_health`**,升级为 Prometheus/ARMS 查询唯一入口,两种模式:
  1. **服务健康快照(默认)**:传 `service`(如 `nfc-.*`),一次调用并行查询 11 条核心 PromQL(request 3 + database 3 + jvm 1 + system 4),返回结构化快照。
  2. **任意指标查询**:传 `query`(PromQL),执行即时查询(最近 10 分钟),用于快照发现异常后深入定位。
- schema 的 `required` 改为空数组(service 或 query 二选一),description 明确"所有 Prometheus 监控相关查询都应使用本工具"。
- handler 内部:先检查 `query` → 走任意查询;否则 `service` → 走快照;两者都无 → 报错提示。

`hermes/skills/library/diagnose_prometheus_anomaly.md`:

- "How to use the tools" 改为**只有一个工具** `prometheus_service_health`,两种模式说明。
- 推荐流程:先 `prometheus_service_health(service="nfc-.*")` 拿快照;仅异常时再传 `query` 深入;通常 1-2 次工具调用即出报告。
- Checking steps / 时间范围章节同步移除旧工具引用。

`hermes/agents/skill_runner.py`:注释更新为 `prometheus_service_health`。

### 验证

- 17 passed(`tests/tools/prometheus/` 9 个 + `tests/tools/skills/` 8 个),无回归。
- 测试新增 query 模式用例:单次调用、错误处理。
- `hermes/` 下 grep `prometheus_instant_query|prometheus_range_query` **无残留**。
- skill 工具集实测只剩 1 个 prometheus 工具:`prometheus_service_health`。
- `./restart.sh` 已重启生效。
